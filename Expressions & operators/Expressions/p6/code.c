#include "stdio.h"

void main(void)
{
	float length,width;
	printf("please enter the length : ");
	scanf("%f", &length);
	printf("please enter the width : ");
	scanf("%f", &width);
	printf("their multiply %f", length*2+width*2);
}